node_modules/
dist/
.DS_Store
.env
.netlify
photos/
student25.zip

# private full backups (contain PII + keys, never commit)
_backup_private/

# DB backup dumps — cloud copy is a GitHub Actions artifact; local copy on the office PC
backups/
_ci_backup/

# scratch / temp
scratch/
node_tmp/
temp_unzip/
*.log

# Local-only legacy archive (kept on disk, not deployed)
_legacy/
public/1clSplash_original.png
